@model IEnumerable<MvcMovie.Models.ProductNew>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductNewName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categories_)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductNewName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categories_.CategoryID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductNewID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductNewID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductNewID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
